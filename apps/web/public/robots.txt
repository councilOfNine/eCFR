User-agent: *
Allow: /

# Generated by src/pages/sitemap.xml.ts from the same route index the build uses, so it can
# never list a page that was not emitted. ~11,100 URLs, well inside the 50,000-URL limit.
Sitemap: https://ecfr-atlas.org/sitemap.xml
